package Oficina.Modelo;

//ENUM = Enumeration = lista de valores fixos que não mudam
//usamos quando temos um conjunto limitado de opções
public enum ECategoria {
    //cada valor é uma constante
    PEQUENO, //carros compactos (ex: fiat uno, gol)
    MEDIO, //carros com o porte médio: (ex: corolla, Civic)
    GRANDE, //SUVs e Caminhonetes (ex: hilux, SW4)
    MOTO, //motocicletas (ex: biz, CG)
    PADRAO //categoria padrão
}
