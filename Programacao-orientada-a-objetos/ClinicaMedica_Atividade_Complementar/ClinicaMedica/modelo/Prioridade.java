package clinica.modelo;

//ENUM: lista de valores fixos para a prioridade da consulta
public enum Prioridade {
    ALTA, //consulta urgente (EX: dor forte, emergência)
    MEDIA, //consulta normal (EX: acompanhamento de rotina)
    BAIXA  //consulta não urgente (EX: retorno para exames)
}
